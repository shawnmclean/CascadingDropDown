desc "Build Project"
task :build => :min do
  
end

desc "Coffeescript compile"
task :coffee do
  
end

desc "Minify the compiled file"
task :min => :coffee do
  #coffee -c -o ./src/idle.coffee ./build/
end